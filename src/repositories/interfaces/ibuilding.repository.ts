import { Building } from "../../entities/building.entity";
import { IGenericRepository } from "./igeneric.repository";

export interface IBuildingRepository extends IGenericRepository<Building> {
    
}